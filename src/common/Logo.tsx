import React from 'react';
import Logo from '../assets/logo.svg';

const LockAndKeyLogo = () => {
    return (
        <img style={{ width: 32, borderRadius: 5 }} src={Logo} alt="logo" />
    )
}

export default LockAndKeyLogo;
