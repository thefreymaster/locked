import { SUPPORTED_LOCATION_LONG, SUPPORTED_LOCATION_LAT } from "../constants";

export const calcIsSupportedLocation = (dispatch: any, dbKey: number) => {
  SUPPORTED_LOCATION_LONG.map(
    (location: { min: number; max: number }, index: number) => {
        
    }
  );
};
